{
  "name": "naukri-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.0",
    "mysql2": "^3.14.3",
    "openai": "^5.13.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "description": "1. Copy `.env.example` to `.env` and fill database credentials and JWT_SECRET. 2. Run `npm install`. 3. Ensure MySQL database `naukri_clone` exists and run the SQL schema in ../db_schema.sql. 4. Start server: `npm run dev` (needs nodemon) or `npm start`.",
  "keywords": [],
  "author": "",
  "license": "ISC"
}
